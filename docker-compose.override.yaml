# Development overrides — auto-loaded by docker/podman compose.
# Remove or rename this file for production-like deployments.
services:
  api:
    volumes:
      - ./backend:/app
      - /app/.venv
  ui:
    environment:
      NGINX_API_KEY_FALLBACK: ${BLACKBEARD_API_KEY:-change-me-in-production}
