apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blackbeard.configmapName" . }}
  labels:
    {{- include "blackbeard.labels" . | nindent 4 }}
data:
  {{- if .Values.litellm.enabled }}
  litellm-config.yaml: |
    {{- .Values.litellm.config | toYaml | nindent 4 }}
  {{- end }}
